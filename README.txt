Once the repository is pulled follow the steps before running the application

1. Create database in Mysql 
2.Create database Table  with attributes id(primary Key), data_points(int)
3.open settings.py file  update the Database name ,user, password
4. Now Run the application
